import React, { useState } from 'react';

function Options() {
    return (
        <div style={styles.content}>
            <h1>Options</h1>
        </div>
    );
}

const styles = {
    content: {
        margin: '0',
        fontFamily: 'font',
        maxWidth: '700px',
        margin: '10px auto',
        padding: '10px',
        fontFamily: "system-ui"
    }
}

export default Options;
